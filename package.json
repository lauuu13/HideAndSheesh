{
  "name": "hideandseek",
  "version": "1.0.0",
  "description": "Local network 3D Hide & Seek prototype (Android)",
  "private": true,
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android"
  },
  "dependencies": {
    "three": "^0.169.0",
    "capacitor-host-server": "file:./plugins/capacitor-host-server"
  },
  "devDependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  }
}
